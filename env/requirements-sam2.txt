# SAM2 跟踪环境
# 创建: conda create -n cr-sam2 python=3.10 -y && conda activate cr-sam2

torch>=2.3
torchvision
numpy
opencv-python-headless
pillow
matplotlib
tqdm
hydra-core
iopath

# SAM2 权重 (~900MB):
#   cd sam2/checkpoints && bash download_ckpts.sh
# 或手动下载 sam2.1_hiera_large.pt 到 sam2/checkpoints/
